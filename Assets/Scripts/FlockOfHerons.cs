using UnityEngine;
using System.Collections;

//This prefab will spawn some Heron ships and assign them to slots in a fleet. The fleet will follow the player, if it can find them. Otherwise, it will fly in circles.

public class FlockOfHerons : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
